# DBAnonymousObjectAnonymousClass-Type-
what is anonymous object and class and usage

///
Anonymous object is used to contain some data, we don’t care the type what a data. We use it once, we spare some memory using him. Anonymous Object, (type) gets only the needed data with no extra data details. Anonymous object, spares Ram memory. Anonymous Object we use it to contain information in it extracted from the DataBase. It eases the ORM work.
